name: Build Crash-utility
on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
jobs:
  x86_64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout
      - name: install tools
        run: sudo apt-get install patch gcc g++ liblzo2-dev libsnappy-dev libzstd-dev wget texinfo bison flex
      - name: make
        run: make -j$(nproc) Warn lzo zstd snappy
  aarch64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout
      - name: install tools
        run: sudo apt-get install patch gcc g++ liblzo2-dev libsnappy-dev libzstd-dev wget texinfo bison flex
      - name: make
        run: make -j$(nproc) Warn lzo zstd snappy
  ppc64le:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout
      - name: install tools
        run: sudo apt-get install patch gcc g++ liblzo2-dev libsnappy-dev libzstd-dev wget texinfo bison flex
      - name: make
        run: make -j$(nproc) Warn lzo zstd snappy
  s390x:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout
      - name: install tools
        run: sudo apt-get install patch gcc g++ liblzo2-dev libsnappy-dev libzstd-dev wget texinfo bison flex
      - name: make
        run: make -j$(nproc) Warn lzo zstd snappy
